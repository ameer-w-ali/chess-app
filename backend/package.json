{
  "name": "backend",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun --hot server.ts",
    "build": "bun build server.ts --outdir dist --target node --minify",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "chess.js": "^1.0.0-beta.8",
    "common": "link:common",
    "uuid": "^10.0.0"
  }
}